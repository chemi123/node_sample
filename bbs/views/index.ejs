<% include ./include/header1 %>
<div class="container">
  <% include ./include/header2 %>
  <div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
      <h1 class="display-4 font-italic">シンプルなBBSだね！</h1>
  </div>

  <main>
    <% for (let i in posts) { %>
      <div class="card mb-4 h-md-250">
        <div class="card-body d-flex flex-column align-items-start">
          <h3 class="mb-3">
            <a class="text-dark" href="<%= posts[i].id %>"><%= posts[i].title %></a>
          </h3>
          <p class="card-text mb-auto"><%= posts[i].description%></p>
          <a href="<%= posts[i].id %>">続きを読む</a>
        </div>
      </div>
    <% } %>
  </main>
  <% include ./include/footer1 %>
</div>
<% include ./include/footer2 %>
